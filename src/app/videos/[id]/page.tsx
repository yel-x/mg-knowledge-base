import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { mockVideos } from "@/data";
import VideoPlaceholder from "@/components/videos/VideoPlaceholder";
export default async function VideoPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const video = mockVideos.find((item) => item.id === id); if (!video) return <div className="py-16 text-center"><h1 className="text-2xl font-semibold">Video not found.</h1><Link href="/videos" className="mt-5 inline-flex rounded-lg bg-[#1e6870] px-4 py-2.5 text-sm font-semibold text-white">Back to Video Tutorials</Link></div>; return <div className="mx-auto max-w-3xl space-y-7"><Link href="/videos" className="inline-flex items-center gap-2 text-sm font-semibold text-[#1e6870]"><ArrowLeft size={16} /> Back to videos</Link><div><p className="eyebrow">Demo video</p><h1 className="mt-2 text-3xl font-semibold">{video.title}</h1><p className="mt-3 text-[#687981]">{video.description}</p></div><div className="grid aspect-video place-items-center rounded-xl bg-[#173944] text-center text-[#c6d8db]">Video placeholder<br />{video.duration}</div><VideoPlaceholder title={video.title} duration={video.duration} /></div>; }
